import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const AuthIndexScreen = () => {
    return (
        <SafeAreaView style={{ flex: 1 }}>
            <Text>This isn the index screen</Text>
        </SafeAreaView>
    )
}

export default AuthIndexScreen;
